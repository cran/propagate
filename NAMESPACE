## functions
export(
propagate,
makeGrad,
makeHess,
evalDerivs,
makeGrad,
makeHess,
mixCov,
cor2cov,
statVec,
skewness,
kurtosis,
fitDistr,
colVarsC,
rowVarsC,
makeDat,
numGrad,
numHess,
predictNLS,
WelchSatter,
bigcor,
interval,
stochContr, 
sobol
)

importFrom("graphics", abline, barplot, boxplot, hist, lines,
           par, title, grid, segments)
importFrom("stats", BIC, coef, cor, cov, D, dcauchy,
           density, df.residual, dgamma, dlnorm, dlogis,
           dnorm, dt, dunif, ks.test, mad, median, pnorm, 
           qbeta, qnorm, qt, quantile, rbinom, rchisq,
           residuals, rexp, rgamma, rnorm, rt, runif, sd,
           shapiro.test, var, vcov, dbeta, dchisq, dexp, df, qqplot, 
           weights, formula, as.formula, cov2cor, complete.cases)
importFrom("utils", flush.console, tail)
import(Rcpp)
importFrom("copula", normalCopula, mvdc, rMvdc, P2p)
importFrom("hdf5r", H5File)
importFrom("minpack.lm", nlsLM, nls.lm, nls.lm.control)
importFrom("crayon", red)

## S3 methods
S3method(print, propagate)
S3method(plot, propagate)
S3method(summary, propagate)
S3method(print, interval)
S3method(print, fitDistr)

useDynLib(propagate, .registration = TRUE)